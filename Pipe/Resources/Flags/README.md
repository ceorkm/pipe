# Country flags

SVG flags from https://github.com/hampusborgos/country-flags (public domain).

Used to show a proxy's exit country once a connection test identifies it.
Files are named by lowercase ISO 3166-1 alpha-2 code, e.g. `us.svg`.
